### 新功能
1. **多上游负载均衡与故障切换**：支持为模型配置多个上游通道，提供灵活的负载均衡策略（按用户随机/按请求随机），内置健康状态检查，任何非成功响应将自动触发故障切换（Failover）。
2. **高精度计费系统**：余额与计价底层架构全面 Decimal 化（采用整数微元存储），彻底消除浮点数计算误差，支持以百万 Token 为单位的精准计价。
3. **免费模型支持**：价格为 0 的模型将自动跳过计费与余额检查环节，即使账户为负余额也能正常放行调用。
4. **全生命周期请求日志**：请求记录系统升级为“单次请求一条记录 + 详细活动时间线”模式，且非流式请求现在也能精准记录完整的响应耗时。
5. **UI 体验焕新**：全面优化了模型路由与上游配置的前端 UI，新增可视化状态标签、操作图标与响应式布局。
6. 协议转换新支持 Responses 与 OpenAI 的双向转换。
7. 支持通过 GitHub Actions 在线自动部署到 Cloudflare。

### 优化与修复
1. 重构欠费拦截逻辑：请求前准确拦截负余额用户（免费模型除外），且前端修复了微小欠费被错误显示为 0.00 的问题。
2. 细化 Failover 错误反馈：当所有备用节点均尝试耗尽后，网关会精确返回带有完整路由上下文的最后一次报错信息。
3. 加入日志轮转机制，减少日志大小占用。
4. 优化设置 UI 并将前后端默认配置对齐统一。
5. 将开源许可证由 GPL v2 更换为了限制更少的 MIT + 署名条款。
6. 修复流式解析中的一些边界断句问题，增加鲁棒性。
7. 修复在线部署时 D1 数据库 ID 无法正确绑定的问题。
